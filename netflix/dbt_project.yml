name: netflix

profile: netflix

seed-paths: ["seeds"]
model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  netflix:
    +database: MOVIELENS
    +materialized: view
    staging:
      +schema: STAGING
      +materialized: view
    dim:
      +schema: ANALYTICS
      +materialized: table
    fact:
      +schema: ANALYTICS
      +materialized: table
    marts:
      +schema: ANALYTICS
      +materialized: table
